<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Рыболовный Мир | Fishing World</title>
    <style>
        :root {
            --primary-dark: #0a1f0a;
            --primary-green: #143d14;
            --glossy-green: #1e5c1e;
            --accent-gold: #d4af37;
            --text-light: #e0e0e0;
            --glass-bg: rgba(20, 61, 20, 0.7);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: radial-gradient(circle at top right, #2d5a2d, #051005);
            color: var(--text-light);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
        }

        /* Glossy Header */
        header {
            background: linear-gradient(180deg, rgba(30, 92, 30, 0.9) 0%, rgba(10, 31, 10, 0.95) 100%);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--glass-border);
            padding: 1rem 2rem;
            position: sticky;
            top: 0;
            z-index: 100;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 20px rgba(0,0,0,0.5);
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: var(--accent-gold);
            text-shadow: 0 0 10px rgba(212, 175, 55, 0.3);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 20px;
        }

        nav button {
            background: transparent;
            border: none;
            color: var(--text-light);
            font-size: 1rem;
            cursor: pointer;
            padding: 8px 16px;
            border-radius: 20px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        nav button:hover, nav button.active {
            background: var(--glossy-green);
            box-shadow: 0 0 15px rgba(30, 92, 30, 0.6);
            color: #fff;
        }

        .user-actions {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .icon-btn {
            background: rgba(255,255,255,0.1);
            border: 1px solid var(--glass-border);
            color: white;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: 0.3s;
            position: relative;
        }

        .icon-btn:hover {
            background: var(--accent-gold);
            color: var(--primary-dark);
        }

        .cart-count {
            position: absolute;
            top: -5px;
            right: -5px;
            background: red;
            color: white;
            font-size: 0.7rem;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
        }

        /* Main Content Area */
        main {
            flex: 1;
            padding: 2rem;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
        }

        .section {
            display: none;
            animation: fadeIn 0.5s ease;
        }

        .section.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Hero Section */
        .hero {
            position: relative;
            border-radius: 20px;
            overflow: hidden;
            height: 500px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            border: 1px solid var(--glass-border);
        }

        .hero img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .hero:hover img {
            transform: scale(1.05);
        }

        .hero-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(to top, var(--primary-dark), transparent);
            padding: 3rem 2rem;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        .hero h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #fff;
            text-shadow: 0 2px 4px rgba(0,0,0,0.8);
        }

        .cta-btn {
            background: linear-gradient(45deg, var(--glossy-green), var(--accent-gold));
            border: none;
            padding: 12px 30px;
            color: var(--primary-dark);
            font-weight: bold;
            border-radius: 30px;
            cursor: pointer;
            font-size: 1.1rem;
            box-shadow: 0 4px 15px rgba(212, 175, 55, 0.4);
            transition: transform 0.2s;
        }

        .cta-btn:hover {
            transform: translateY(-3px);
        }

        /* Products Grid */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .card {
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            border-radius: 15px;
            padding: 1rem;
            transition: 0.3s;
            display: flex;
            flex-direction: column;
        }

        .card:hover {
            background: rgba(30, 92, 30, 0.8);
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
        }

        .card-img {
            height: 200px;
            background: #0f2b0f;
            border-radius: 10px;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        
        .card-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .card h3 {
            margin-bottom: 0.5rem;
            color: var(--accent-gold);
        }

        .card p {
            font-size: 0.9rem;
            color: #aaa;
            margin-bottom: 1rem;
            flex: 1;
        }

        .price-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: auto;
        }

        .price {
            font-size: 1.2rem;
            font-weight: bold;
            color: #fff;
        }

        .add-btn {
            background: var(--glossy-green);
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.2s;
        }

        .add-btn:hover {
            background: var(--accent-gold);
            color: var(--primary-dark);
        }

        /* Articles & Tips */
        .article-card {
            background: linear-gradient(135deg, rgba(30, 92, 30, 0.6), rgba(10, 31, 10, 0.8));
            border-left: 4px solid var(--accent-gold);
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            border-radius: 0 15px 15px 0;
        }

        .article-card h2 {
            color: var(--accent-gold);
            margin-bottom: 0.5rem;
        }

        .tag {
            display: inline-block;
            background: rgba(255,255,255,0.1);
            padding: 4px 10px;
            border-radius: 15px;
            font-size: 0.8rem;
            margin-bottom: 10px;
            color: #ccc;
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.8);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(5px);
        }

        .modal-content {
            background: linear-gradient(135deg, #1a3d1a, #0a1f0a);
            padding: 2rem;
            border-radius: 20px;
            width: 90%;
            max-width: 400px;
            border: 1px solid var(--glass-border);
            box-shadow: 0 0 30px rgba(20, 61, 20, 0.5);
            position: relative;
        }

        .close-modal {
            position: absolute;
            top: 15px;
            right: 20px;
            font-size: 1.5rem;
            cursor: pointer;
            color: #aaa;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--accent-gold);
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            background: rgba(0,0,0,0.3);
            border: 1px solid var(--glossy-green);
            border-radius: 5px;
            color: white;
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--accent-gold);
        }

        /* Cart Sidebar */
        .cart-sidebar {
            position: fixed;
            top: 0;
            right: -400px;
            width: 350px;
            height: 100%;
            background: #0f2b0f;
            z-index: 200;
            transition: 0.3s ease;
            padding: 2rem;
            border-left: 1px solid var(--glossy-green);
            box-shadow: -10px 0 30px rgba(0,0,0,0.5);
            display: flex;
            flex-direction: column;
        }

        .cart-sidebar.open {
            right: 0;
        }

        .cart-items {
            flex: 1;
            overflow-y: auto;
            margin: 1rem 0;
        }

        .cart-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .checkout-btn {
            width: 100%;
            padding: 15px;
            background: var(--accent-gold);
            border: none;
            color: var(--primary-dark);
            font-weight: bold;
            border-radius: 10px;
            cursor: pointer;
            margin-top: 1rem;
        }

        @media (max-width: 768px) {
            header {
                flex-direction: column;
                gap: 1rem;
            }
            nav ul {
                flex-wrap: wrap;
                justify-content: center;
            }
            .hero h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header>
        <div class="logo">
            <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2a10 10 0 1 0 10 10H12V2z"></path><path d="M12 12 2.1 12.1"></path><path d="M12 12 9.3 19.3"></path></svg>
            Рыболовный Мир
        </div>
        <nav>
            <ul>
                <li><button class="active" onclick="showSection('home')">Главная</button></li>
                <li><button onclick="showSection('shop')">Магазин</button></li>
                <li><button onclick="showSection('stories')">Истории</button></li>
                <li><button onclick="showSection('tips')">Советы</button></li>
            </ul>
        </nav>
        <div class="user-actions">
            <button class="icon-btn" onclick="toggleCart()">
                🛒
                <span class="cart-count" id="cartCount">0</span>
            </button>
            <button class="icon-btn" onclick="toggleModal('loginModal')">
                👤
            </button>
        </div>
    </header>

    <!-- Main Content -->
    <main>
        <!-- Home Section -->
        <section id="home" class="section active">
            <div class="hero">
                <img src="https://image.qwenlm.ai/public_source/6f48f824-c160-4d61-aebe-e398fb2a2b0f/191a4bbaa-a540-4dd9-9905-d39ced44c804.png" alt="Рыбалка на рассвете">
                <div class="hero-overlay">
                    <h1>Искусство Тишины</h1>
                    <p style="margin-bottom: 20px; font-size: 1.1rem; max-width: 600px;">
                        Откройте для себя лучшие снасти, проверенные техники и сообщество единомышленников. 
                        Ваша идеальная рыбалка начинается здесь.
                    </p>
                    <button class="cta-btn" onclick="showSection('shop')">Перейти в каталог</button>
                </div>
            </div>
            
            <div style="margin-top: 3rem;">
                <h2 style="color: var(--accent-gold); margin-bottom: 1rem;">Популярные товары</h2>
                <div class="grid-container" id="featured-products">
                    <!-- Populated by JS -->
                </div>
            </div>
        </section>

        <!-- Shop Section -->
        <section id="shop" class="section">
            <h2 style="color: var(--accent-gold); margin-bottom: 1rem; border-bottom: 1px solid var(--glass-border); padding-bottom: 10px;">Каталог Снастей</h2>
            <div class="grid-container" id="shop-products">
                <!-- Populated by JS -->
            </div>
        </section>

        <!-- Stories Section -->
        <section id="stories" class="section">
            <h2 style="color: var(--accent-gold); margin-bottom: 2rem;">Истории и Новости</h2>
            
            <div class="article-card">
                <span class="tag">Новости</span>
                <h2>Рекордный улов на Ладоге</h2>
                <p>Группа энтузиастов установила новый рекорд по ловле судака в ночное время. Читайте подробности о технике и приманках, которые сработали лучше всего...</p>
                <button style="margin-top: 10px; background: none; border: 1px solid var(--accent-gold); color: var(--accent-gold); padding: 5px 15px; border-radius: 15px; cursor: pointer;">Читать далее</button>
            </div>

            <div class="article-card">
                <span class="tag">История</span>
                <h2>Легенда о Золотом Карпе</h2>
                <p>Древняя история рыбацкой деревни, где, по преданию, в глубоком омуте живет рыба, исполняющая желания. Миф или реальность? Разбираемся в фактах...</p>
                <button style="margin-top: 10px; background: none; border: 1px solid var(--accent-gold); color: var(--accent-gold); padding: 5px 15px; border-radius: 15px; cursor: pointer;">Читать далее</button>
            </div>
        </section>

        <!-- Tips Section -->
        <section id="tips" class="section">
            <h2 style="color: var(--accent-gold); margin-bottom: 2rem;">Рекомендации Профессионалов</h2>
            
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem;">
                <div class="card">
                    <h3>🎣 Выбор места</h3>
                    <p>Ищите перепады глубин и коряжники. Хищник любит засады. Используйте эхолот для точного картирования дна.</p>
                </div>
                <div class="card">
                    <h3>🌤 Влияние погоды</h3>
                    <p>Перед грозой клев обычно усиливается. При резком падении давления рыба уходит на глубину и становится пассивной.</p>
                </div>
                <div class="card">
                    <h3>🪱 Работа с приманкой</h3>
                    <p>Не бойтесь экспериментировать с проводкой. Иногда самая простая остановка (пауза) провоцирует атаку лучше, чем активная игра.</p>
                </div>
            </div>
        </section>
    </main>

    <!-- Login Modal -->
    <div id="loginModal" class="modal">
        <div class="modal-content">
            <span class="close-modal" onclick="toggleModal('loginModal')">&times;</span>
            <h2 style="color: var(--accent-gold); margin-bottom: 1.5rem; text-align: center;">Личный кабинет</h2>
            <form onsubmit="event.preventDefault(); alert('Вход выполнен!'); toggleModal('loginModal');">
                <div class="form-group">
                    <label>Email</label>
                    <input type="email" placeholder="fisherman@example.com" required>
                </div>
                <div class="form-group">
                    <label>Пароль</label>
                    <input type="password" placeholder="••••••••" required>
                </div>
                <button type="submit" class="checkout-btn">Войти</button>
                <p style="text-align: center; margin-top: 1rem; font-size: 0.9rem; color: #aaa;">
                    Нет аккаунта? <a href="#" style="color: var(--accent-gold);">Зарегистрироваться</a>
                </p>
            </form>
        </div>
    </div>

    <!-- Cart Sidebar -->
    <div class="cart-sidebar" id="cartSidebar">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
            <h2 style="color: var(--accent-gold);">Корзина</h2>
            <button onclick="toggleCart()" style="background: none; border: none; color: white; font-size: 1.5rem; cursor: pointer;">&times;</button>
        </div>
        <div class="cart-items" id="cartItemsContainer">
            <p style="text-align: center; color: #aaa; margin-top: 2rem;">Корзина пуста</p>
        </div>
        <div style="border-top: 1px solid var(--glass-border); padding-top: 1rem;">
            <div style="display: flex; justify-content: space-between; margin-bottom: 1rem; font-size: 1.2rem; font-weight: bold;">
                <span>Итого:</span>
                <span id="cartTotal">0 ₽</span>
            </div>
            <button class="checkout-btn" onclick="alert('Переход к оплате...')">Оформить заказ</button>
        </div>
    </div>

    <script>
        // Data
        const products = [
            { id: 1, name: "Спиннинг Predator X", price: 12500, img: "https://via.placeholder.com/300x200/1a3d1a/d4af37?text=Rod" },
            { id: 2, name: "Катушка Shimano Pro", price: 8900, img: "https://via.placeholder.com/300x200/1a3d1a/d4af37?text=Reel" },
            { id: 3, name: "Набор воблеров", price: 3200, img: "https://via.placeholder.com/300x200/1a3d1a/d4af37?text=Lures" },
            { id: 4, name: "Эхолот FishFinder", price: 15000, img: "https://via.placeholder.com/300x200/1a3d1a/d4af37?text=Sonar" },
            { id: 5, name: "Палатка для зимней рыбалки", price: 22000, img: "https://via.placeholder.com/300x200/1a3d1a/d4af37?text=Tent" },
            { id: 6, name: "Ящик рыболовный", price: 4500, img: "https://via.placeholder.com/300x200/1a3d1a/d4af37?text=Box" },
        ];

        let cart = [];

        // Init
        document.addEventListener('DOMContentLoaded', () => {
            renderProducts('featured-products', products.slice(0, 3));
            renderProducts('shop-products', products);
        });

        // Functions
        function showSection(sectionId) {
            document.querySelectorAll('.section').forEach(sec => sec.classList.remove('active'));
            document.getElementById(sectionId).classList.add('active');
            
            document.querySelectorAll('nav button').forEach(btn => btn.classList.remove('active'));
            const navMap = {'home': 0, 'shop': 1, 'stories': 2, 'tips': 3};
            if(navMap[sectionId] !== undefined) {
                document.querySelectorAll('nav button')[navMap[sectionId]].classList.add('active');
            }
            window.scrollTo(0,0);
        }

        function toggleModal(modalId) {
            const modal = document.getElementById(modalId);
            modal.style.display = modal.style.display === 'flex' ? 'none' : 'flex';
        }

        function toggleCart() {
            document.getElementById('cartSidebar').classList.toggle('open');
        }

        function renderProducts(containerId, items) {
            const container = document.getElementById(containerId);
            container.innerHTML = items.map(item => `
                <div class="card">
                    <div class="card-img">
                        <img src="${item.img}" alt="${item.name}">
                    </div>
                    <h3>${item.name}</h3>
                    <p>Профессиональное оборудование для настоящих ценителей тишины и удачи.</p>
                    <div class="price-row">
                        <span class="price">${item.price.toLocaleString()} ₽</span>
                        <button class="add-btn" onclick="addToCart(${item.id})">В корзину</button>
                    </div>
                </div>
            `).join('');
        }

        function addToCart(id) {
            const product = products.find(p => p.id === id);
            cart.push(product);
            updateCartUI();
            
            const btn = event.target;
            const originalText = btn.innerText;
            btn.innerText = "✓";
            btn.style.background = "#d4af37";
            btn.style.color = "#0a1f0a";
            setTimeout(() => {
                btn.innerText = originalText;
                btn.style.background = "";
                btn.style.color = "";
            }, 1000);
        }

        function removeFromCart(index) {
            cart.splice(index, 1);
            updateCartUI();
        }

        function updateCartUI() {
            document.getElementById('cartCount').innerText = cart.length;
            const container = document.getElementById('cartItemsContainer');
            const totalEl = document.getElementById('cartTotal');
            
            let total = 0;
            
            if (cart.length === 0) {
                container.innerHTML = '<p style="text-align: center; color: #aaa; margin-top: 2rem;">Корзина пуста</p>';
            } else {
                container.innerHTML = cart.map((item, index) => {
                    total += item.price;
                    return `
                        <div class="cart-item">
                            <div>
                                <div style="font-weight: bold; color: white;">${item.name}</div>
                                <div style="font-size: 0.9rem; color: #aaa;">${item.price.toLocaleString()} ₽</div>
                            </div>
                            <button onclick="removeFromCart(${index})" style="background: none; border: none; color: #ff4444; cursor: pointer;">✕</button>
                        </div>
                    `;
                }).join('');
            }
            
            totalEl.innerText = total.toLocaleString() + ' ₽';
        }

        window.onclick = function(event) {
            const modal = document.getElementById('loginModal');
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }
    </script>
</body>
</html>

